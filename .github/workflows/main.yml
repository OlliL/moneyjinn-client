name: Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Checkout Server repo
        uses: actions/checkout@v4
        with:
          repository: OlliL/moneyjinn-server

      - name: Install Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 17

      - name: Build API
        run: ls -l ; mvn -Dopenapi.inputSpec=moneyjinn-server/moneyjinn-server/src/main/resources/openapi.json compile
        
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
      - run: npm ci
      - run: npm run build --if-present
      - run: npm test
